<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <org.angmarch.views.NiceSpinner
        android:id="@+id/spinner_routine"
        android:layout_marginTop="20dp"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        />

    <ListView
        android:layout_marginTop="20dp"
        android:id="@+id/activity_list"
        android:layout_width="300dp"
        android:layout_height="350dp"
        app:layout_constraintTop_toBottomOf="@+id/spinner_routine"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        >
    </ListView>
    <TextView
        android:id="@+id/edit_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="10dp"
        android:text="수정"
        android:textSize="14dp"
        android:textColor="#000"
        app:layout_constraintTop_toBottomOf="@+id/activity_list"
        app:layout_constraintRight_toLeftOf="@+id/remove_btn"
        />
    <TextView
        android:id="@+id/remove_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="삭제"
        android:textSize="14dp"
        android:textColor="#000"
        app:layout_constraintTop_toBottomOf="@+id/activity_list"
        app:layout_constraintRight_toRightOf="@+id/activity_list"
        />

    <Button
        android:id="@+id/start_btn"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="시작"
        android:textColor="@color/my_white"
        android:background="#000"
        android:textSize="20dp"
        android:layout_marginBottom="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        />


</android.support.constraint.ConstraintLayout>